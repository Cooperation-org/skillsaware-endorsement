import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  /* config options here */
  // Uncomment the line below for Docker/ECS deployment
  // output: 'standalone'
}

export default nextConfig
